/*
package com.sathish.productservice.services.filteringService;

public class FilterFactory {
    public static Filter getFilter(String filterType) {
        if (filterType == null) {
            return null;
        }
        return switch (filterType.toLowerCase()) {
            case "branch" -> new BranchFilter();
            case "ram" -> new RamFilter();
            default -> null;
        };
    }
}
*/
